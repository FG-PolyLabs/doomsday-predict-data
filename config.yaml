gcp:
  project_id: fg-polylabs

gcs:
  bucket: fg-polylabs-doomsday
  # Path prefix inside the bucket where data files are uploaded
  data_prefix: data/

bigquery:
  dataset: doomsday
  # Table names must match the filenames in data/ (without extension)
  tables:
    - markets
